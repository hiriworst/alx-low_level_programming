#include <stdio.h>
/**
 * main - print alphabet in lowercase
 * followed by a newline
 * return: Always 0
 */
int main(void)
{
	char ch;
	for (ch  = 'a'; ch <= 'z'; ch++);
	putchar ch);
	putchar ('\n');
}
